# LibraryNet Backend

Spring Boot REST API using Java 17, Spring Data JPA, validation, PostgreSQL, and the original DSA implementations.

Run:

```powershell
.\mvnw.cmd spring-boot:run
```

Test:

```powershell
.\mvnw.cmd clean test
```
